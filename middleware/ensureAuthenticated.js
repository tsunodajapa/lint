/**
 * @description esse metodo define x coisa
 * @param {Object} user
 * @param {Stirng} user.name
 * @param {Number} user.age
 * @returns
 */
const ensureAuthenticated = user => { };

module.exports = ensureAuthenticated;
